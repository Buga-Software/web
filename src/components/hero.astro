<section class="space-y-3 px-1 flex flex-col items-center justify-center py-16 md:pb-32 md:pt-28 w-full text-center text-[var(--whitebg)]">
  <span class="px-4 py-1 text-xs md:text-sm font-medium rounded-full border bg-white/10 backdrop-blur-xs border-white/30">
    ✨ Para negocios y empresas
  </span>

  <h1 class="text-balance text-3xl md:text-5xl font-bold max-w-4xl text-[var(--white)]">
    Optimiza procesos y crece con soluciones digitales
  </h1>

  <p class="text-pretty text-sm md:text-lg text-center max-w-2xl">
    Nos encargamos de darte las mejores soluciones con
    tecnologías modernas brindándote el mejor soporte
  </p>

  <div class="flex flex-col gap-4 items-center">
    <a href="/contacto" class="glow-border monospace px-6 py-3 text-sm font-semibold rounded-xl bg-[var(--white)] text-[var(--black)] border-2 border-[var(--white)] hover:bg-[var(--black)]/10 hover:backdrop-blur-sm hover:text-[var(--white)] transition-all duration-300 ease-in-out shadow-lg hover:shadow-xl">
      Solicita tu propuesta gratis
    </a>
    <span class="text-xs opacity-80">Sin compromiso ni tarjeta requerida</span>
  </div>
</section>


<style>
.glow-border {
  position: relative;
  overflow: visible;
}

.glow-border::before {
  content: "";
  position: absolute;
  inset: -1px;
  border-radius: inherit;
  padding: 3px;
  background: linear-gradient(
    90deg,
    transparent,
    #fef9ff,
    transparent
  );
  background-size: 200% 100%;
  animation: glow-move 2s linear infinite;
  -webkit-mask: 
    linear-gradient(#fff 0 0) content-box, 
    linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
}

.glow-border::after {
  content: "";
  position: absolute;
  inset: -1px;
  border-radius: inherit;
  background: radial-gradient(circle, rgba(255,255,255,0.8) 0%, transparent 70%);
  background-size: 200% 100%;
  animation: glow-move 3s linear infinite;
  filter: blur(8px);
  z-index: -1;
}

.glow-border:hover::after {
  opacity: 0;
}

@keyframes glow-move {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}
</style>
