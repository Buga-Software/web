<section
    class="flex flex-col items-center justify-center py-16 md:pb-32 md:pt-28 w-full text-center text-[var(--whitebg)]"
>
    <span
        class="px-4 py-1 mb-3 text-sm md:text-md font-medium rounded-full border bg-white/5 backdrop-blur-xs border-white/10"
    >
        🥇 Para negocios o empresas
    </span>

    <h1
        class="text-3xl text-balance md:text-6xl max-w-4xl text-[var(--white)] font-medium"
    >
        Optimiza procesos y crece con soluciones digitales
    </h1>

    <p class="text-sm md:text-xl text-pretty text-center mt-6 max-w-2xl">
        Impulsa tu negocio con herramientas digitales modernas, <br />
        soporte experto y resultados medibles.
    </p>

    <div class="mt-8 flex flex-col gap-4 items-center">
        <a
            href="#contacto"
            class="glow-border px-6 py-2 text-base md:text-lg font-medium rounded-full bg-[var(--white)] text-[var(--black)] border-2 border-[var(--white)] hover:bg-[var(--black)]/10 hover:backdrop-blur-sm hover:text-[var(--white)] transition-all duration-300 ease-in-out shadow-lg hover:shadow-xl"
        >
            Solicita tu propuesta gratis
        </a>
        <span class="text-sm opacity-80"
            >Sin compromiso ni tarjeta requerida</span
        >
    </div>
</section>

<style>
    .glow-border {
        position: relative;
        overflow: visible;
    }

    .glow-border::before {
        content: "";
        position: absolute;
        inset: -1px;
        border-radius: inherit;
        padding: 3px;
        background: linear-gradient(90deg, transparent, #fef9ff, transparent);
        background-size: 200% 100%;
        animation: glow-move 2s linear infinite;
        -webkit-mask:
            linear-gradient(#fff 0 0) content-box,
            linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
    }

    .glow-border::after {
        content: "";
        position: absolute;
        inset: -1px;
        border-radius: inherit;
        background: radial-gradient(
            circle,
            rgba(255, 255, 255, 0.8) 0%,
            transparent 70%
        );
        background-size: 200% 100%;
        animation: glow-move 3s linear infinite;
        filter: blur(8px);
        z-index: -1;
    }

    .glow-border:hover::after {
        opacity: 0;
    }

    @keyframes glow-move {
        0% {
            background-position: 200% 0;
        }
        100% {
            background-position: -200% 0;
        }
    }
</style>
